{
  "name": "app-store-monorepo",
  "private": true,
  "workspaces": [
    "apps/application-storage"
  ],
  "scripts": {
    "start": "npm run -w @fantasyui/application-storage start",
    "make": "npm run -w @fantasyui/application-storage make",
    "lint": "npm run -w @fantasyui/application-storage lint",
    "audit": "npm audit --workspaces --include-workspace-root",
    "audit:fix": "npm audit fix --workspaces --include-workspace-root"
  }
}
